{
  "name": "cloudflare",
  "private": true,
  "description": "Cloudflare packages for Wrangler config sync, Worker deploys, and Worker-native forms",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter './packages/*' --if-present run build",
    "typecheck": "pnpm --filter './packages/*' --if-present run typecheck && tsc --noEmit",
    "test": "vitest run",
    "changeset": "changeset",
    "changeset:status": "changeset status",
    "version": "changeset version",
    "release:check": "pnpm run typecheck && pnpm run test && pnpm run build && pnpm --filter @ozby/wrangler-sync exec npm pack --dry-run && pnpm --filter @ozby/cloudflare exec npm pack --dry-run",
    "release:publish": "pnpm run release:check && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.6.0",
    "vite": "^8.0.14",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.5.0"
}
